using UnityEngine;

/*
 * This Script Is Made So I can Test Different Ideas
 * Mostly By using Debug.Log() to understand how this or that works
 */
/*
 * Debug.Log outputs the string result of the entered parameters in the Console
 * it is very good way for finding bugs and understanding
 * how what work or if something happens or not 
 */
public class TestingScript : MonoBehaviour
{

    // Start is called before the first frame update
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
        // var radius = testObejct.position - Vector3.one;
        // var degree = testObejct.eulerAngles;
        // var arcX = radius.x * degree.x * ForArc;
        // var arcY = radius.y * degree.y * ForArc;
        // var arcZ = radius.z * degree.z * ForArc;
        // Debug.Log("arcX: " + arcX);
        // Debug.Log("arcY: " + arcY);
        // Debug.Log("arcZ: " + arcZ);
    }
}